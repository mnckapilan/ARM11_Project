#include "emulator_registers.h"

#include <stdio.h>
#include <stdint.h>

/* Need to create a function which creates a register, zeroing it */

/* Need to create a data structure / type which stores the contents of a register */

/* Need to create a function to write to a register */

/* Need to create a function to read from a register */

/* Possibly a function to zero a register (XOR with itself) */
